<h1>Editing Limit scope</h1>

<% form_for(@limit_scope) do |form| %>
  <%= render :partial => 'form', :locals => {:form => form} %>
  <p><%= form.submit 'Update' %></p>
<% end %>

<p>
  <%= link_to 'Show', @limit_scope %>
  |
  <%= link_to 'Back', limit_scopes_path %>
</p>
