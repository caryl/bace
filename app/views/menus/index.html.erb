<h1> Listing Menus</h1>

<table>
  <tr>
    <th><%=meta_name :name%></th>
    <th><%=meta_name :permission_id%></th>
    <th><%=meta_name :parent_id%></th>
    <th><%=meta_name :econ%></th>
    <th><%=meta_name :url%></th>
    <th><%=meta_name :remark%></th>
  </tr>

  <% for menu in @menus %>
  <tr>
    <td><%='--' * menu.level%><%= h menu.name %></td>
    <td><%= h menu.permission.try(:name) %></td>
    <td><%= h menu.parent.try(:name) %></td>
    <td><%= h menu.icon %></td>
    <td><%= h menu.url %></td>
    <td><%= h menu.remark %></td>
    <td><%= link_to 'Show', menu %></td>
    <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
    <td><%= link_to 'Destroy', menu, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
  <% end %>
</table>

<p><%= link_to 'New Menu', new_menu_path %></p>
